{
  "name": "github-issue-triage-agent",
  "version": "0.1.0",
  "description": "Autonomous GitHub issue-triage agent (TypeScript + Claude Agent SDK) that classifies real issues and applies labels/comments via the GitHub API.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "triage": "tsx src/cli.ts",
    "eval": "tsx eval/run_eval.ts",
    "test": "node --import tsx --test tests/agent.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@octokit/rest": "^21.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
